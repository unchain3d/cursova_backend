services:
  backend:
    build: .
    container_name: backend
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    command: >
      bash -c "uvicorn app:app --host 0.0.0.0 --port 8000"
    restart: unless-stopped